# The Vanishing Integer

You are given an array nums containing n distinct integers taken from the range [0, n].
Because the array has length n, exactly one integer from that range is missing.
Find the missing integer without using sorting, and aim for linear time and constant extra space.